import { ContentCopy, PlayArrow } from "@mui/icons-material";
import { Button, Chip, Container, LinearProgress, Paper, Stack, Typography } from "@mui/material";
import AuthenticatedPage from "fitness/components/AuthenticatedPage";
import { copyTemplate, createWorkout, getTemplate } from "fitness/utils/spec";
import { WorkoutTemplateDetail } from "fitness/utils/types";
import { useRouter } from "next/router";
import { useEffect, useState } from "react";
export default function TemplateDetailPage() { const router = useRouter(); const [item, setItem] = useState<WorkoutTemplateDetail | null>(null); const id = typeof router.query.id === "string" ? router.query.id : ""; useEffect(() => { if (id) getTemplate(id).then(setItem); }, [id]); if (!item) return <AuthenticatedPage><Container sx={{ py: 4 }}><LinearProgress /></Container></AuthenticatedPage>; return <AuthenticatedPage><Container maxWidth="md" sx={{ py: 4 }}><Stack spacing={3}><Paper sx={{ p: 4 }}><Stack direction="row" justifyContent="space-between" gap={2}><div><Chip label={item.visibility} /><Typography variant="h4" mt={1}>{item.title}</Typography><Typography color="text.secondary">By {item.owner.name || "FitWell user"}</Typography></div><Stack><Button variant="contained" startIcon={<PlayArrow />} onClick={async () => { const result = await createWorkout({ title: item.title, date: new Date(), templateId: item.id }); router.push(`/workouts/${result.id}/edit`); }}>Start workout</Button><Button startIcon={<ContentCopy />} onClick={async () => { const result = await copyTemplate(item.id); router.push(`/templates/${result.id}`); }}>Copy</Button></Stack></Stack>{item.description && <Typography mt={3}>{item.description}</Typography>}<Typography mt={2}>{item.estimatedDurationM ?? "—"} minutes · {item.difficulty} · {item.useCount} uses</Typography></Paper><Typography variant="h6">Plan</Typography>{item.exercises.map((target) => <Paper key={target.exerciseId} sx={{ p: 3 }}><Typography fontWeight={600}>{target.exercise?.name}</Typography><Typography color="text.secondary">{target.targetSets} sets × {target.minReps ?? "—"}{target.maxReps ? `–${target.maxReps}` : ""} reps · rest {target.restSeconds ?? "—"} sec</Typography></Paper>)}</Stack></Container></AuthenticatedPage>; }
